[package]
name = "berth-node"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Berth node daemon"

[dependencies]
berth-protocol = { path = "../berth-protocol" }
bollard = "0.21"
futures-util = "0.3"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }
