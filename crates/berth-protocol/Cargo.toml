[package]
name = "berth-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Computer-session protocol types"

[dependencies]
base64 = "0.22"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
